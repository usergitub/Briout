import 'package:flutter/material.dart';

class OrdonnanceScreen extends StatelessWidget {
  const OrdonnanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Page Ordonnance"),
      ),
    );
  }
}